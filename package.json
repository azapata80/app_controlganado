{
  "name": "ganaderia-el-viejo-browser-tests",
  "private": true,
  "scripts": {
    "test:browser": "playwright test tests/browser.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
